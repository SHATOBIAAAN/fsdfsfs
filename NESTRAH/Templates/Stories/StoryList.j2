{% extends "base/base.j2" %}


{% block head %}
    <link rel="stylesheet" href="{{ static('css/FormContainer.css') }}">
    <link rel="stylesheet" href="{{ static('css/StoryList.css') }}">
    <link rel="stylesheet" href="{{ static('css/NewStory.css') }}">
    <link rel="stylesheet" href="{{ static('css/Base.css') }}">
    <link rel="stylesheet" href="{{ static('css/MainPage.css') }}">
    <style>
        .media-link {
            color: #7bed64;
            text-decoration: none;
            position: relative;
            transition: color 0.3s;
        }
        
        .media-link:hover {
            color: #4ec93f;
            text-decoration: underline;
        }
        
        /* Стили для навигационных стрелок */
        .nav-button {
            position: absolute;
            top: 50%;
            transform: translateY(-50%);
            background: rgba(0, 0, 0, 0.5);
            color: white;
            border: none;
            border-radius: 50%;
            width: 32px;
            height: 32px;
            font-size: 16px;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            z-index: 10;
            -webkit-appearance: none;
            -webkit-tap-highlight-color: transparent;
            transition: background-color 0.3s, opacity 0.3s;
            padding: 0;
            line-height: 1;
        }

        .nav-button::before {
            position: relative;
            top: -1px;
        }

        .nav-button.prev::before {
            content: '❮';
        }

        .nav-button.next::before {
            content: '❯';
        }

        .nav-button.prev {
            left: 10px;
        }

        .nav-button.next {
            right: 10px;
        }

        .nav-button:active {
            background: rgba(0, 0, 0, 0.7);
        }

        /* Специальные стили для iOS */
        @supports (-webkit-touch-callout: none) {
            .nav-button {
                padding: 0;
                margin: 0;
                display: grid;
                place-items: center;
            }
        }
        
        /* Стили для мобильных устройств */
        @media (max-width: 768px) {
            .nav-button {
                width: 28px;
                height: 28px;
                font-size: 14px;
            }
        }
    </style>
{% endblock %}

{% block content %}
    <!-- Всплывающее уведомление о модерации -->
    <div class="notification-popup" id="moderationNotification">
        <div class="notification-icon">✓</div>
        <div class="notification-content">
            <div class="notification-title">История отправлена на модерацию</div>
            <div class="notification-message">Ваша история будет опубликована после проверки администратором</div>
            <div class="notification-click-hint">Нажмите, чтобы перейти в профиль</div>
        </div>
        <div class="notification-close" id="closeNotification">&times;</div>
    </div>
    
    <div class="main-info">
        <div class="main-info-text">
        <h1 class="main-title">ЛЕНТА СЛУЧАЕВ</h1>
        <p class="main-description">Здесь собраны самые дикие,  нелепые и смешеные
истории. Читай, голосуй и не  забывай, что в следущий
раз неудача может настигнуть тебя 😉!</p>
        </div>
       <div class="filter-container">
            <div class="filter-header">
                <div class="filter-item" id="sortFilter">
                    <span class="filter-label">
                        {% if current_sort == 'new' %}
                            Новые
                        {% elif current_sort == 'popular' %}
                            Популярные
                        {% elif current_sort == 'liked' %}
                            Понравившиеся
                        {% elif current_sort == 'old' %}
                            Старые
                        {% else %}
                            Фильтры
                        {% endif %}
                    </span>
                    <div class="filter-dropdown">
                        <ul class="filter-list">
                            <li class="filter-option {% if current_sort == 'new' %}selected{% endif %}" data-value="new">Новые</li>
                            <li class="filter-option {% if current_sort == 'old' %}selected{% endif %}" data-value="old">Старые</li>
                            <li class="filter-option {% if current_sort == 'popular' %}selected{% endif %}" data-value="popular">Популярные</li>
                            <li class="filter-option {% if current_sort == 'liked' %}selected{% endif %}" data-value="liked">Понравившиеся</li>
                        </ul>
                    </div>
                </div>
                
                <div class="filter-item" id="multiCategoryToggle">
                    <span class="filter-label">Категории</span>
                </div>
            </div>
            
            <!-- Всплывающее окно выбора нескольких категорий -->
            <div class="category-area" id="categoryArea">
                <div class="category-content">
                    <button class="close-category-area" id="closeCategoryArea">&times;</button>
                    <div class="category-header">
                        <h3 class="category-title">Выберите категории</h3>
                    </div>
                    <input type="text" class="category-search-box" id="categorySearch" placeholder="Поиск категории...">
                    <div class="category-list-container">
                        <ul class="category-list" id="categoryList">
                            {% for category in categories %}
                            <li class="category-item" data-id="{{ category.id }}" data-name="{{ category.name }}">{{ category.name }}</li>
                            {% endfor %}
                        </ul>
                    </div>
                    <div class="category-selected-display" id="categorySelectedDisplay">
                        <span class="no-category-selected">Категории не выбраны</span>
                    </div>
                    <div class="category-actions">
                        <button type="button" class="apply-category" id="applyCategoryBtn">Применить</button>
                    </div>
                </div>
            </div>
            
            <div class="categories-filter-container" id="activeFiltersContainer">
                <div class="active-filters" id="activeFilters"></div>
            </div>
            
            <input type="hidden" id="selectedSort" name="sort" value="{{ current_sort|default('new') }}">
            <input type="hidden" id="selectedCategories" name="categories" value="{{ current_categories|default('') }}">
            
            <div class="container">
                <!-- Кнопки навигации вне контейнера (добавляем проверку на наличие историй) -->
                {% if page_obj and page_obj|length > 1 %}
                <button class="nav-button prev" id="prevSlide" aria-label="Предыдущий слайд"></button>
                <button class="nav-button next" id="nextSlide" aria-label="Следующий слайд"></button>
                {% endif %}
                
                <div class="story-slider-container">
                    <div class="story-slider" id="storySlider">
                        {% if page_obj %}
                            {% for story in page_obj %}
                            <div class="story-slide">
                                <div class="container-story" data-story-id="{{ story.id }}">
                                    <div class="container-story-name">
                                        <h1>{{ story.title }}</h1>
                                    </div>
                                     <div class="container-story-name-text">
                                        <p>{{ story.content|truncate(300) }}</p>
                                    </div>
                                    <div class="story-footer">
                                        <div class="story-info">
                                            <span class="story-author"><p class="story-info-text-green">Автор:</p>{% if story.user.profile.nickname|length > 15 %}{{ story.user.profile.nickname[:15] }}...{% else %}{{ story.user.profile.nickname }}{% endif %}</span>
                                            <span class="story-author">
                                                <p class="story-info-text-green">Медиа:</p>
                                                {% if story.media_files.exists() %}
                                                <a href="{{ url('story_media', story.id) }}" class="media-link">Открыть</a>
                                                {% else %}
                                                отсутствует
                                                {% endif %}
                                            </span>
                                            <span class="story-author"><p class="story-info-text-green">Категория:</p>
                                                {% if story.categories.exists() %}
                                                    {% for category in story.categories.all() %}
                                                        <span class="story-category-tag">{{ category.name }}</span>
                                                    {% endfor %}
                                                {% elif story.category %}
                                                    {{ story.category.name }}
                                                {% else %}
                                                    Без категории
                                                {% endif %}
                                            </span>
                                            <span class="story-author"><p class="story-info-text-green">Дата:</p>{{ story.created_at.strftime('%d.%m.%Y') }}</span>
                                        </div>
                                        <div class="story-stats-container">
                                        <div class="story-stats comment-stats" data-story-id="{{ story.id }}">
                                            <span class="stats-count">{{ story.comments.count() }}</span>
                                            <span class="comment-icon">💬</span>
                                        </div>
                                        <div class="story-stats like-stats" data-story-id="{{ story.id }}">
                                            <span class="stats-count">{{ story.likes.count() }}</span>
                                            <span class="like-icon {% if request.user.is_authenticated and request.user in story.likes.all() %}active{% endif %}">👍</span>
                                        </div>
                                        <div class="story-stats dislike-stats" data-story-id="{{ story.id }}">
                                            <span class="stats-count">{{ story.dislikes.count() }}</span>
                                            <span class="dislike-icon {% if request.user.is_authenticated and request.user in story.dislikes.all() %}active{% endif %}">👎</span>
                                        </div>
                                        </div>
                                    </div>
                                    <div class="story-read-more"><a href="/stories/{{ story.id }}/" class="read-more">Читать полностью</a></div>
                                </div>
                            </div>
                            {% endfor %}
                        {% else %}
                            <div class="story-slide">
                                <div class="container-story">
                                    <div class="empty-state">
                                        <h2>Истории не найдены</h2>
                                        <p>Пока здесь нет историй. Вы можете быть первым, кто поделится своей историей!</p>
                                    </div>
                                </div>
                            </div>
                        {% endif %}
                    </div>
                </div>
                {% if page_obj and page_obj|length > 0 %}
            <div class="slide-counter-container">
                <div class="slide-counter">
                    <span class="counter-current editable-number" id="currentSlide" contenteditable="true" 
                         title="Нажмите для ввода номера слайда" role="textbox" aria-label="Номер слайда">1</span>
                    <span class="counter-separator">/</span>
                    <span class="counter-total">{{ page_obj|length }}</span>
                    <div class="progress-bar" id="slideProgress"></div>
                </div>
                
                <div class="pagination-info">
                    Страница <span class="current editable-number" id="currentPage" contenteditable="true" 
                         title="Нажмите для изменения номера страницы" role="textbox" aria-label="Номер страницы">{{ current_page }}</span> из {{ total_pages }}
                </div>
            </div>
            {% endif %}
                    </div>
                    
       </div>
    </div>
    
    <script>
        // Определяем, поддерживает ли браузер passive event listeners
        let passiveSupported = false;
        try {
          const options = {
            get passive() {
              passiveSupported = true;
              return true;
            }
          };
          window.addEventListener("test", null, options);
          window.removeEventListener("test", null, options);
        } catch(err) {
          passiveSupported = false;
        }
        
        // Параметры для событий
        const passiveEventOptions = passiveSupported ? { passive: true } : false;
        
        document.addEventListener('DOMContentLoaded', function() {
            // Отключение работы клавиши Tab на странице
            document.addEventListener('keydown', function(e) {
                if (e.key === 'Tab') {
                    e.preventDefault();
                }
            });
            
            // Получаем элементы фильтров
            const sortFilter = document.getElementById('sortFilter');
            const selectedSortInput = document.getElementById('selectedSort');
            
            // Функция для открытия/закрытия выпадающего списка
            function toggleDropdown(filterItem) {
                const dropdown = filterItem.querySelector('.filter-dropdown');
                const isOpen = dropdown.classList.contains('visible');
                
                // Закрываем все открытые выпадающие списки
                document.querySelectorAll('.filter-dropdown.visible').forEach(el => {
                    if (el !== dropdown) {
                        el.classList.remove('visible');
                    }
                });
                
                // Переключаем видимость текущего выпадающего списка
                dropdown.classList.toggle('visible');
            }
            
            // Обработчики кликов на фильтры
            sortFilter.addEventListener('click', function() {
                toggleDropdown(this);
            });
            
            // Обработчик выбора опции в выпадающем списке
            document.querySelectorAll('.filter-option').forEach(option => {
                option.addEventListener('click', function() {
                    const value = this.dataset.value;
                    const filterItem = this.closest('.filter-item');
                    const filterLabel = filterItem.querySelector('.filter-label');
                    const dropdown = filterItem.querySelector('.filter-dropdown');
                    
                    // Определяем, какой фильтр был выбран
                    if (filterItem.id === 'sortFilter') {
                        selectedSortInput.value = value;
                        filterLabel.textContent = this.textContent;
                    }
                    
                    // Закрываем выпадающий список
                    dropdown.classList.remove('visible');
                    
                    // Отправляем запрос на фильтрацию
                    applyFilters();
                });
            });
            
            // Функция для применения фильтров (отправки запроса)
            function applyFilters() {
                const sort = selectedSortInput.value;
                
                // Получаем текущие параметры URL
                const urlParams = new URLSearchParams(window.location.search);
                const params = [];
                
                // При смене сортировки всегда начинаем с первой страницы,
                // так как набор результатов полностью меняется
                // НЕ сохраняем параметр страницы при смене сортировки
                
                // Добавляем параметр сортировки
                if (sort && sort !== 'new') {
                    params.push(`sort=${sort}`);
                }
                
                // Сохраняем параметр categories, если он уже был в URL
                const categoriesParam = urlParams.get('categories');
                if (categoriesParam) {
                    params.push(`categories=${categoriesParam}`);
                }
                
                // Формируем URL с параметрами
                let url = window.location.pathname;
                if (params.length > 0) {
                    url += '?' + params.join('&');
                }
                
                // Перенаправляем на новый URL
                window.location.href = url;
            }
            
            // Закрытие выпадающих списков при клике вне них
            document.addEventListener('click', function(event) {
                if (!event.target.closest('.filter-item')) {
                    document.querySelectorAll('.filter-dropdown').forEach(dropdown => {
                        dropdown.classList.remove('visible');
                    });
                }
            });
            
            // Слайдер для историй
            const slider = document.getElementById('storySlider');
            const slides = document.querySelectorAll('.story-slide');
            const prevButton = document.getElementById('prevSlide');
            const nextButton = document.getElementById('nextSlide');
            
            // Глобальные переменные для слайдера
                let currentIndex = 0;
            let isAnimating = false;
            let totalSlides = slides.length;

            // Объявляем функцию showSlide в глобальной области видимости
            window.showSlide = function(index) {
                    // Если анимация уже выполняется, ничего не делаем
                    if (isAnimating) return;
                    isAnimating = true;
                    
                    // Определяем направление анимации счетчика
                    const direction = index > currentIndex ? 'down' : 'up';
                    
                    // Проверяем, нужно ли переходить на другую страницу
                    if (index < 0) {
                        // Если первый слайд, проверяем есть ли предыдущая страница
                        goToPrevPage();
                        isAnimating = false;
                        return;
                    } else if (index >= totalSlides) {
                        // Если последний слайд, проверяем есть ли следующая страница
                        goToNextPage();
                        isAnimating = false;
                        return;
                    }
                    
                    // Обновляем текущий индекс
                    currentIndex = index;
                    
                    // Сохраняем позицию слайда
                    saveSlidePosition(currentIndex);
                    
                    // Убираем класс active у всех слайдов
                    slides.forEach(slide => {
                        slide.classList.remove('active');
                    });
                    
                    // Добавляем класс active к текущему слайду с анимацией
                    slides[currentIndex].classList.add('active');
                    
                    // Перемещаем слайдер с плавной анимацией
                    slider.style.transform = `translateX(-${currentIndex * 100}%)`;
                    
                    // Обновляем счетчик слайдов с анимацией
                    updateCounter(currentIndex + 1, direction);
                    
                    // Обновляем прогресс-бар
                    updateProgressBar(currentIndex + 1, totalSlides);
                    
                    // Управляем видимостью стрелок в зависимости от позиции
                    updateArrowsVisibility();
                
                // Сохраняем текущее состояние
                saveCurrentState();
                    
                    // Сбрасываем флаг анимации через время, равное длительности перехода
                    setTimeout(() => {
                        isAnimating = false;
                    }, 600);
            };

            // Если есть слайды и их больше одного
            if (slides.length > 1) {
                // Инициализируем слайдер сразу
                initializeSlider();

                // Восстанавливаем состояние при загрузке страницы
                    setTimeout(() => {
                    applyRestoredState();
                }, 100);

                // Добавляем сохранение состояния при навигации
                if (prevButton) {
                prevButton.addEventListener('click', () => {
                        if (!isAnimating) {
                            window.showSlide(currentIndex - 1);
                        }
                });
                }
                
                if (nextButton) {
                nextButton.addEventListener('click', () => {
                        if (!isAnimating) {
                            window.showSlide(currentIndex + 1);
                        }
                    });
                }

                // Сохраняем состояние перед уходом со страницы
                window.addEventListener('beforeunload', saveCurrentState);
            }
                
                // Добавляем обработчики для редактируемых номеров слайдов и страниц
                const currentSlideElement = document.getElementById('currentSlide');
                const currentPageElement = document.getElementById('currentPage');
                
                // Обработчик для перехода на слайд при вводе номера
                if (currentSlideElement) {
                    // Сохраняем оригинальное значение при получении фокуса
                    currentSlideElement.addEventListener('focus', function() {
                        // Запоминаем ширину элемента перед редактированием
                        const originalWidth = this.offsetWidth;
                        this.style.minWidth = originalWidth + 'px';
                        
                        this.dataset.originalValue = this.textContent;
                        
                        // Добавляем класс для анимации клика
                        this.classList.add('clicked');
                        setTimeout(() => {
                            this.classList.remove('clicked');
                        }, 500);
                    });
                    
                    // Обработка ввода - только цифры
                    currentSlideElement.addEventListener('keydown', function(e) {
                        if (e.key === 'Enter') {
                            e.preventDefault();
                            this.blur();
                            return;
                        }
                        
                        // Разрешаем только цифры, Backspace, Delete, Tab, стрелки
                        if (!/^\d$/.test(e.key) && 
                            !['Backspace', 'Delete', 'Tab', 'ArrowLeft', 'ArrowRight'].includes(e.key)) {
                            e.preventDefault();
                        }
                    });
                    
                    // Применение изменений при потере фокуса
                    currentSlideElement.addEventListener('blur', function() {
                        // Удаляем фиксированную ширину
                        this.style.minWidth = '';
                        
                        const newValue = parseInt(this.textContent, 10);
                        // Проверяем, что введено число и оно в допустимом диапазоне
                        if (isNaN(newValue) || newValue < 1 || newValue > totalSlides) {
                            this.textContent = this.dataset.originalValue; // Восстанавливаем оригинальное значение
                        } else {
                            // Переходим к выбранному слайду
                        window.showSlide(newValue - 1);
                        }
                    });
                }
                
                // Обработчик для перехода на страницу при вводе номера
                if (currentPageElement) {
                    // Сохраняем оригинальное значение при получении фокуса
                    currentPageElement.addEventListener('focus', function() {
                        // Запоминаем ширину элемента перед редактированием
                        const originalWidth = this.offsetWidth;
                        this.style.minWidth = originalWidth + 'px';
                        
                        this.dataset.originalValue = this.textContent;
                        
                        // Добавляем класс для анимации клика
                        this.classList.add('clicked');
                        setTimeout(() => {
                            this.classList.remove('clicked');
                        }, 500);
                    });
                    
                    // Обработка ввода - только цифры
                    currentPageElement.addEventListener('keydown', function(e) {
                        if (e.key === 'Enter') {
                            e.preventDefault();
                            this.blur();
                            return;
                        }
                        
                        // Разрешаем только цифры, Backspace, Delete, Tab, стрелки
                        if (!/^\d$/.test(e.key) && 
                            !['Backspace', 'Delete', 'Tab', 'ArrowLeft', 'ArrowRight'].includes(e.key)) {
                            e.preventDefault();
                        }
                    });
                    
                    // Применение изменений при потере фокуса
                    currentPageElement.addEventListener('blur', function() {
                        // Удаляем фиксированную ширину
                        this.style.minWidth = '';
                        
                        const newValue = parseInt(this.textContent, 10);
                        const totalPages = {{ total_pages }};
                        
                        // Проверяем, что введено число и оно в допустимом диапазоне
                        if (isNaN(newValue) || newValue < 1 || newValue > totalPages) {
                            this.textContent = this.dataset.originalValue; // Восстанавливаем оригинальное значение
                        } else {
                            // Если выбрана та же страница, на которой мы находимся, ничего не делаем
                            const currentPage = parseInt('{{ current_page }}', 10);
                            if (newValue === currentPage) {
                                this.textContent = currentPage;
                                return;
                            }
                            
                            // Перейти на новую страницу
                            navigateToPage(newValue);
                        }
                    });
                }
                
                // Функция для перехода на выбранную страницу с сохранением параметров фильтрации
                function navigateToPage(page) {
                    const urlParams = new URLSearchParams(window.location.search);
                    const currentPage = parseInt(urlParams.get('page') || '1');
                    
                    // Создаем новый объект параметров
                    const newParams = new URLSearchParams();
                    
                    // Добавляем параметр страницы
                    newParams.set('page', page.toString());
                    
                    // Сохраняем параметры сортировки и категорий
                    const sort = urlParams.get('sort');
                    const categories = urlParams.get('categories');
                    
                    if (sort) newParams.set('sort', sort);
                    if (categories) newParams.set('categories', categories);
                    
                    // Добавляем start_from_end только при переходе на предыдущую страницу
                    if (page < currentPage) {
                        newParams.set('start_from_end', 'true');
                    }
                    
                    // Очищаем состояние перед переходом
                    localStorage.removeItem('storyListState');
                    sessionStorage.removeItem('isRedirecting');
                    sessionStorage.setItem('intentionalNavigation', 'true');
                    
                    // Показываем индикатор загрузки
                    showPageLoading();
                    
                    // Переходим на новую страницу
                    window.location.href = '/stories/?' + newParams.toString();
                }
                
                // Функция для отображения индикатора загрузки при переходе между страницами
                function showPageLoading() {
                    let pageLoading = document.querySelector('.page-loading');
                    
                    if (!pageLoading) {
                        pageLoading = document.createElement('div');
                        pageLoading.className = 'page-loading';
                        
                        const loader = document.createElement('div');
                        loader.className = 'loader';
                        pageLoading.appendChild(loader);
                        
                        document.body.appendChild(pageLoading);
                        
                        // Форсируем перерисовку
                        void pageLoading.offsetWidth;
                    }
                    
                    pageLoading.classList.add('active');
                }
                
                // Обработка свайпов на мобильных устройствах
                let touchStartX = 0;
                let touchEndX = 0;
                let isSwiping = false;
                let swipeTranslate = 0;
                
                slider.addEventListener('touchstart', e => {
                    isSwiping = true;
                    touchStartX = e.changedTouches[0].screenX;
                    // Останавливаем стандартный переход
                    slider.style.transition = 'none';
                }, passiveEventOptions);
                
                slider.addEventListener('touchmove', e => {
                if (!isSwiping) return;
                    
                    // Вычисляем смещение от начала свайпа
                    const currentX = e.changedTouches[0].screenX;
                    swipeTranslate = currentX - touchStartX;
                    
                    // Ограничиваем смещение
                    const maxTranslate = window.innerWidth * 0.3;
                    if (Math.abs(swipeTranslate) > maxTranslate) {
                        swipeTranslate = swipeTranslate > 0 ? maxTranslate : -maxTranslate;
                    }
                    
                    // Применяем смещение с резистентностью на краях
                    const resistance = 0.4;
                    const baseTransform = -currentIndex * 100;
                    let dragOffset = swipeTranslate / window.innerWidth * 100;
                    
                    // Добавляем сопротивление если свайп выходит за пределы слайдера
                    if ((currentIndex === 0 && swipeTranslate > 0) || 
                        (currentIndex === totalSlides - 1 && swipeTranslate < 0)) {
                        dragOffset *= resistance;
                    }
                    
                    slider.style.transform = `translateX(${baseTransform + dragOffset}%)`;
                }, passiveEventOptions);
                
                slider.addEventListener('touchend', e => {
                if (!isSwiping) return;
                    
                    // Возвращаем анимацию
                    slider.style.transition = 'transform 0.6s cubic-bezier(0.33, 1, 0.68, 1)';
                    
                    touchEndX = e.changedTouches[0].screenX;
                    const threshold = window.innerWidth * 0.15; // 15% экрана
                    
                    // Проверяем, можно ли перейти влево/вправо (учитываем скрытые стрелки)
                    const isFirstPage = parseInt(new URLSearchParams(window.location.search).get('page') || '1') === 1;
                    const isLastPage = parseInt(new URLSearchParams(window.location.search).get('page') || '1') >= {{ total_pages }};
                    const canGoLeft = !(currentIndex === 0 && isFirstPage);
                    const canGoRight = !(currentIndex === totalSlides - 1 && isLastPage);
                    
                    if (touchStartX - touchEndX > threshold && canGoRight) {
                        // Свайп влево - следующий слайд
                    window.showSlide(currentIndex + 1);
                    } else if (touchEndX - touchStartX > threshold && canGoLeft) {
                        // Свайп вправо - предыдущий слайд
                    window.showSlide(currentIndex - 1);
                    } else {
                        // Возвращаем текущий слайд на место
                        slider.style.transform = `translateX(-${currentIndex * 100}%)`;
                    }
                    
                    isSwiping = false;
                    swipeTranslate = 0;
                }, passiveEventOptions);
                
                // Обработка прерывания свайпа
                slider.addEventListener('touchcancel', e => {
                    if (!isSwiping) return;
                    
                    // Возвращаем анимацию
                    slider.style.transition = 'transform 0.6s cubic-bezier(0.33, 1, 0.68, 1)';
                    
                    // Возвращаем слайд в исходное положение
                    slider.style.transform = `translateX(-${currentIndex * 100}%)`;
                    
                    isSwiping = false;
                    swipeTranslate = 0;
                }, passiveEventOptions);
                
                // Обработка нажатий клавиш (влево/вправо)
                document.addEventListener('keydown', e => {
                if (isAnimating) return;
                    
                    // Проверяем, можно ли перейти влево/вправо (учитываем скрытые стрелки)
                    const isFirstPage = parseInt(new URLSearchParams(window.location.search).get('page') || '1') === 1;
                    const isLastPage = parseInt(new URLSearchParams(window.location.search).get('page') || '1') >= {{ total_pages }};
                    const canGoLeft = !(currentIndex === 0 && isFirstPage);
                    const canGoRight = !(currentIndex === totalSlides - 1 && isLastPage);
                    
                    if (e.key === 'ArrowLeft' && canGoLeft) {
                    isAnimating = true;
                    window.showSlide(currentIndex - 1);
                        setTimeout(() => {
                        isAnimating = false;
                        }, 650);
                    } else if (e.key === 'ArrowRight' && canGoRight) {
                    isAnimating = true;
                    window.showSlide(currentIndex + 1);
                        setTimeout(() => {
                        isAnimating = false;
                        }, 650);
                    }
                });
                
                // Функция проверки статуса пагинации
                function checkPaginationStatus() {
                    // Получаем текущие параметры URL
                    const urlParams = new URLSearchParams(window.location.search);
                    let currentPage = parseInt(urlParams.get('page') || '1');
                    
                    // Проверяем наличие параметров сортировки и категорий
                    const hasSort = urlParams.has('sort');
                    const hasCategories = urlParams.has('categories');
                    
                    // Проверяем необходимость отображения информации о пагинации
                    const paginationInfo = document.querySelector('.pagination-info');
                    if (paginationInfo) {
                        paginationInfo.style.display = 'flex';
                    }
                    
                    // Проверяем доступность перехода вперед/назад
                    updateArrowsVisibility();
                }
                
                // Функция для перехода на следующую страницу при большом количестве историй
                function goToNextPage() {
                    // Если текущий слайд последний, переходим на следующую страницу
                    if (currentIndex === totalSlides - 1) {
                        // Получаем текущие параметры URL
                        const urlParams = new URLSearchParams(window.location.search);
                        let currentPage = parseInt(urlParams.get('page') || '1');
                        
                        // Сохраняем все остальные параметры (сортировка, категории)
                        const sort = urlParams.get('sort');
                        const categories = urlParams.get('categories');
                        
                        // Создаем новый объект параметров
                        const newParams = new URLSearchParams();
                        
                        // Добавляем параметр страницы
                        newParams.set('page', (currentPage + 1).toString());
                        
                        // Добавляем остальные параметры, если они есть
                        if (sort) newParams.set('sort', sort);
                        if (categories) newParams.set('categories', categories);
                        
                        // Показываем индикатор загрузки
                        showPageLoading();
                        
                        // Перенаправляем пользователя
                        setTimeout(() => {
                            window.location.href = window.location.pathname + '?' + newParams.toString();
                        }, 300);
                    }
                }
                
                // Функция для перехода на предыдущую страницу
                function goToPrevPage() {
                    // Если текущий слайд первый, переходим на предыдущую страницу
                    if (currentIndex === 0) {
                        // Получаем текущие параметры URL
                        const urlParams = new URLSearchParams(window.location.search);
                        let currentPage = parseInt(urlParams.get('page') || '1');
                        
                        // Не переходим, если это первая страница
                        if (currentPage <= 1) {
                            return;
                        }
                        
                        // Сохраняем все остальные параметры (сортировка, категории)
                        const sort = urlParams.get('sort');
                        const categories = urlParams.get('categories');
                        
                        // Создаем новый объект параметров
                        const newParams = new URLSearchParams();
                        
                        // Добавляем параметр страницы
                        newParams.set('page', (currentPage - 1).toString());
                        
                    // Добавляем параметр для начала с конца
                    newParams.set('start_from_end', 'true');
                        
                        // Добавляем остальные параметры, если они есть
                        if (sort) newParams.set('sort', sort);
                        if (categories) newParams.set('categories', categories);
                        
                        // Показываем индикатор загрузки
                        showPageLoading();
                        
                        // Перенаправляем пользователя
                            window.location.href = window.location.pathname + '?' + newParams.toString();
                    }
                }
                
                // Проверяем URL параметры для определения начальной позиции слайдера
                function checkForInitialPosition() {
                    // Проверяем, есть ли параметр from_end в URL
                    const urlParams = new URLSearchParams(window.location.search);
                    const fromEnd = urlParams.get('from_end');
                    const slideIndex = urlParams.get('slide_index');
                    const fromStoryId = urlParams.get('from_story_id');
                    
                    // Если есть параметр slide_index (возврат со страницы комментариев)
                    if (slideIndex !== null) {
                        const index = parseInt(slideIndex);
                        if (!isNaN(index) && index >= 0 && index < totalSlides) {
                            // Показываем сохраненный слайд
                        window.showSlide(index);
                            
                            // Удаляем параметры из URL без перезагрузки страницы
                            urlParams.delete('slide_index');
                            urlParams.delete('from_story_id');
                            const newUrl = window.location.pathname + 
                                        (urlParams.toString() ? '?' + urlParams.toString() : '');
                            window.history.replaceState({}, document.title, newUrl);
                            return;
                        }
                    }
                    // Если есть fromStoryId, но нет slideIndex - пытаемся найти историю по ID
                    else if (fromStoryId !== null) {
                        // Ищем слайд с нужным storyId
                        for (let i = 0; i < slides.length; i++) {
                            const storyContainer = slides[i].querySelector('.container-story');
                            if (storyContainer && storyContainer.getAttribute('data-story-id') === fromStoryId) {
                            window.showSlide(i);
                                
                                // Удаляем параметр из URL без перезагрузки страницы
                                urlParams.delete('from_story_id');
                                const newUrl = window.location.pathname + 
                                            (urlParams.toString() ? '?' + urlParams.toString() : '');
                                window.history.replaceState({}, document.title, newUrl);
                                return;
                            }
                        }
                    }
                    // Если есть параметр from_end (возврат с предыдущей страницы)
                    else if (fromEnd === '1') {
                        // Начинаем с последнего слайда
                    window.showSlide(totalSlides - 1);
                        
                        // Удаляем параметр из URL без перезагрузки страницы
                        urlParams.delete('from_end');
                        const newUrl = window.location.pathname + 
                                      (urlParams.toString() ? '?' + urlParams.toString() : '');
                        window.history.replaceState({}, document.title, newUrl);
                    } else {
                        // Стандартная инициализация с первого слайда
                    window.showSlide(0);
                    }
                }
                
                // Функция для обновления видимости стрелок навигации
                function updateArrowsVisibility() {
                    if (!prevButton || !nextButton) return;
                    
                    // Показываем обе стрелки по умолчанию
                    prevButton.style.opacity = '1';
                    prevButton.style.pointerEvents = 'auto';
                    nextButton.style.opacity = '1';
                    nextButton.style.pointerEvents = 'auto';
                    
                    // Получаем текущую страницу и параметры фильтрации
                    const urlParams = new URLSearchParams(window.location.search);
                    const isFirstPage = parseInt(urlParams.get('page') || '1') === 1;
                    const isLastPage = parseInt(urlParams.get('page') || '1') >= {{ total_pages }};
                    
                    // Если мы находимся на первой странице с фильтрами категорий,
                    // проверяем общее количество страниц для этих фильтров
                    const categoriesParam = urlParams.get('categories');
                    const hasCategoryFilter = categoriesParam && categoriesParam.length > 0;
                    
                    // Если первый слайд и первая страница
                    if (currentIndex === 0 && isFirstPage) {
                        prevButton.style.opacity = '0';
                        prevButton.style.pointerEvents = 'none';
                    }
                    
                    // Если последний слайд и последняя страница
                    if (currentIndex === totalSlides - 1 && isLastPage) {
                        nextButton.style.opacity = '0';
                        nextButton.style.pointerEvents = 'none';
                    }
                }
                
                // Сохраняем индекс слайда при переходе на страницу комментариев
                document.querySelectorAll('.comment-stats').forEach(function(commentBtn) {
                    commentBtn.addEventListener('click', function() {
                        // Сохраняем текущий индекс слайда в localStorage перед переходом
                        localStorage.setItem('lastSlideIndex', currentIndex);
                    });
                });

            // Вспомогательные функции
            function formatNumber(num) {
                if (num < 1000) return num;
                if (num < 10000) return (num/1000).toFixed(1) + 'k';
                if (num < 1000000) return Math.round(num/1000) + 'k';
                return (num/1000000).toFixed(1) + 'M';
            }

            function updateProgressBar(current, total) {
                const progressBar = document.getElementById('slideProgress');
                if (!progressBar) return;
                
                // Плавная анимация прогресс-бара
                const percentage = (current / total) * 100;
                progressBar.style.width = `${percentage}%`;
                
                // Обновляем общее количество с форматированием
                const totalElement = document.querySelector('.counter-total');
                if (totalElement) {
                    totalElement.textContent = formatNumber(total);
                }
            }

            function updateCounter(current, direction) {
                const counterElement = document.getElementById('currentSlide');
                if (!counterElement) return;
                
                // Удаляем предыдущие классы анимации
                counterElement.classList.remove('counter-animation-up', 'counter-animation-down');
                
                // Сохраняем contenteditable состояние
                const wasEditable = counterElement.contentEditable === 'true';
                if (wasEditable) {
                    counterElement.contentEditable = 'false';
                }
                
                // Устанавливаем значение без анимации, если это инициализация
                if (!direction) {
                    counterElement.textContent = formatNumber(current);
                } else {
                    // Форсируем перерисовку DOM
                    void counterElement.offsetWidth;
                    
                    // Добавляем анимацию в зависимости от направления
                    counterElement.classList.add(`counter-animation-${direction}`);
                    
                    // Меняем значение счетчика с форматированием
                    counterElement.textContent = formatNumber(current);
                }
                
                // Восстанавливаем contenteditable состояние
                setTimeout(() => {
                    if (wasEditable) {
                        counterElement.contentEditable = 'true';
                    }
                }, 500);
            }

            function adjustCounterSizeForLargeNumbers(total) {
                const counterContainer = document.querySelector('.slide-counter');
                if (!counterContainer) return;
                
                // Если больше 999, уменьшаем размер шрифта
                if (total >= 1000) {
                    counterContainer.classList.add('big-number');
                }
            }

            function saveSlidePosition(index) {
                localStorage.setItem('lastSlideIndex', index);
                // Дополнительно сохраняем ID текущей истории
                const currentSlide = slides[index];
                if (currentSlide) {
                    const storyContainer = currentSlide.querySelector('.container-story');
                    if (storyContainer) {
                        const storyId = storyContainer.getAttribute('data-story-id');
                        if (storyId) {
                            localStorage.setItem('lastStoryId', storyId);
                        }
                    }
                }
            }

            // Функция для проверки и очистки параметров URL
            function cleanupUrlParameters() {
                const urlParams = new URLSearchParams(window.location.search);
                const slideIndex = urlParams.get('slide_index');
                const fromStoryId = urlParams.get('from_story_id');
                const startFromEnd = urlParams.get('start_from_end');
                
                // Создаем новый объект параметров
                const newParams = new URLSearchParams();
                
                // Сохраняем основные параметры
                const page = urlParams.get('page');
                const sort = urlParams.get('sort');
                const categories = urlParams.get('categories');
                
                if (page) newParams.set('page', page);
                if (sort) newParams.set('sort', sort);
                if (categories) newParams.set('categories', categories);
                
                // Если нужно начать с последнего слайда
                if (startFromEnd === 'true') {
                    currentIndex = slides.length - 1;
                    window.showSlide(currentIndex);
                    
                    // Удаляем параметр start_from_end
                    urlParams.delete('start_from_end');
                }
                
                // Если есть параметры возврата со страницы комментариев
                if (slideIndex !== null) {
                    const index = parseInt(slideIndex);
                    if (!isNaN(index) && index >= 0 && index < slides.length) {
                        setTimeout(() => {
                            window.showSlide(index);
                        }, 100);
                    }
                }
                
                // Заменяем URL без перезагрузки страницы
                const newUrl = window.location.pathname + 
                              (newParams.toString() ? '?' + newParams.toString() : '');
                window.history.replaceState({}, '', newUrl);
            }

            // Функция для применения сохраненного состояния
            function applyRestoredState() {
                // Проверяем, была ли это намеренная навигация
                if (sessionStorage.getItem('intentionalNavigation')) {
                    sessionStorage.removeItem('intentionalNavigation');
                    return;
                }

                const state = restoreState();
                if (!state) {
                    // Если нет сохраненного состояния, проверяем параметры URL
                    cleanupUrlParameters();
                    return;
                }

                const currentUrlParams = new URLSearchParams(window.location.search);
                const currentPage = currentUrlParams.get('page') || '1';
                
                // Не выполняем автоматический редирект, если мы уже на нужной странице
                if (state.page === currentPage) {
                    if (state.slide) {
                        const slideIndex = parseInt(state.slide) - 1;
                        if (window.showSlide && typeof window.showSlide === 'function') {
                            window.showSlide(slideIndex);
                        }
                    }
                    localStorage.removeItem('storyListState');
                    return;
                }

                // Очищаем состояние перед редиректом
                localStorage.removeItem('storyListState');
            }

            // Функция для определения направления навигации
            function determineNavigationDirection(targetPage) {
                const currentPage = parseInt(window.location.pathname.split('/').pop()) || 1;
                sessionStorage.setItem('previousPage', currentPage);
                return targetPage < currentPage;
            }

            // Функция для инициализации слайдера
            function initializeSlider() {
                if (slides.length <= 1) return;

                // Скрываем счетчик на время инициализации
                const counterElement = document.getElementById('currentSlide');
                const counterContainer = document.querySelector('.slide-counter');
                if (counterContainer) {
                    counterContainer.style.opacity = '0';
                }

                // Проверяем параметр start_from_end
                const urlParams = new URLSearchParams(window.location.search);
                const startFromEnd = urlParams.get('start_from_end') === 'true';
                
                if (startFromEnd) {
                    currentIndex = slides.length - 1;
                    slides[currentIndex].classList.add('active');
                    updateProgressBar(currentIndex + 1, totalSlides);
                    slider.style.transform = `translateX(-${currentIndex * 100}%)`;
                    
                    // Устанавливаем правильное значение счетчика
                    if (counterElement) {
                        counterElement.textContent = totalSlides;
                    }
                    
                    // Удаляем параметр из URL
                    urlParams.delete('start_from_end');
                    const newUrl = window.location.pathname + (urlParams.toString() ? '?' + urlParams.toString() : '');
                    window.history.replaceState({}, '', newUrl);
                } else {
                    currentIndex = 0;
                    slides[0].classList.add('active');
                    updateProgressBar(1, totalSlides);
                    
                    // Устанавливаем значение для первого слайда
                    if (counterElement) {
                        counterElement.textContent = '1';
                    }
                }

                // Проверяем, если слишком большие числа, добавляем специальный класс
                adjustCounterSizeForLargeNumbers(totalSlides);
                
                // Проверка наличия страниц для навигации
                checkPaginationStatus();
                
                // Обновляем видимость стрелок
                updateArrowsVisibility();

                // Плавно показываем счетчик после инициализации
                if (counterContainer) {
                    setTimeout(() => {
                        counterContainer.style.transition = 'opacity 0.3s ease';
                        counterContainer.style.opacity = '1';
                    }, 100);
                }
            }

            const urlParams = new URLSearchParams(window.location.search);
            const startFromEnd = urlParams.get('start_from_end') === 'true';
            
            if (startFromEnd && slider) {
                currentIndex = slides.length - 1;
                showSlide(currentIndex);
                
                // Очищаем URL от параметра
                urlParams.delete('start_from_end');
                const newUrl = window.location.pathname + (urlParams.toString() ? '?' + urlParams.toString() : '');
                window.history.replaceState({}, '', newUrl);
            }
        });
    </script>
    
    <!-- JavaScript для всплывающего уведомления о модерации -->
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Проверяем, была ли отправлена история на модерацию
            {% if request.session.story_submitted %}
                // Получаем элемент уведомления
                const notification = document.getElementById('moderationNotification');
                const closeBtn = document.getElementById('closeNotification');
                
                // Показываем уведомление с небольшой задержкой
                setTimeout(() => {
                    notification.classList.add('show');
                }, 500);
                
                // Скрываем уведомление через 6 секунд
                const autoHideTimeout = setTimeout(() => {
                    hideNotification();
                }, 6000);
                
                // Функция для скрытия уведомления с анимацией
                function hideNotification() {
                    notification.style.transition = 'opacity 0.5s ease, transform 0.5s ease';
                    notification.style.opacity = '0';
                    notification.style.transform = 'translateY(-20px)';
                    
                    // После завершения анимации убираем класс show
                    setTimeout(() => {
                        notification.classList.remove('show');
                        notification.style = '';  // Сбрасываем инлайн-стили
                    }, 500);
                }
                
                // Добавляем обработчик клика для кнопки закрытия
                closeBtn.addEventListener('click', function(e) {
                    e.stopPropagation(); // Предотвращаем всплытие события клика
                    hideNotification();
                    clearTimeout(autoHideTimeout);
                });
                
                // Добавляем обработчик клика для перехода в профиль по клику на основную часть уведомления
                notification.addEventListener('click', function(e) {
                    // Проверяем, что клик был не по кнопке закрытия
                    if (e.target !== closeBtn && !closeBtn.contains(e.target)) {
                        // Сначала скрываем уведомление
                        hideNotification();
                        
                        // Отменяем автоматическое скрытие
                        clearTimeout(autoHideTimeout);
                        
                        // Перенаправляем на страницу профиля
                        setTimeout(() => {
                            window.location.href = "{{ url('profile') }}";
                        }, 300); // Небольшая задержка для анимации
                    }
                });
                
                // Удаляем флаг из сессии, чтобы не показывать уведомление при следующей загрузке страницы
                fetch("{{ url('clear_notification') }}", { 
                    method: 'POST', 
                    headers: { 
                        'X-CSRFToken': '{{ csrf_token }}' 
                    } 
                });
            {% endif %}

            // Инициализируем форматирование больших чисел и активные состояния
            document.querySelectorAll('.stats-count').forEach(formatLargeNumber);
            
            // Если пользователь авторизован, устанавливаем активные состояния для лайков и дизлайков
            {% if request.user.is_authenticated %}
                document.querySelectorAll('.story-stats').forEach(function(button) {
                    const iconElement = button.querySelector('.like-icon, .dislike-icon');
                    if (iconElement && iconElement.classList.contains('active')) {
                        if (iconElement.classList.contains('like-icon')) {
                            button.classList.add('active-like');
                        } else if (iconElement.classList.contains('dislike-icon')) {
                            button.classList.add('active-dislike');
                        }
                    }
                });
            {% endif %}
            
            // Добавляем обработчики для кнопок лайка и дизлайка
            document.querySelectorAll('.like-stats, .dislike-stats').forEach(function(button) {
                button.addEventListener('click', function(e) {
                    e.preventDefault();
                    // Предотвращаем всплытие события
                    e.stopPropagation();
                    
                    const storyId = this.getAttribute('data-story-id');
                    const isLike = this.classList.contains('like-stats');
                    
                    // Добавляем класс для анимации только к этой кнопке
                    // Предварительно блокируем кнопку чтобы избежать множественных кликов
                    if (this.classList.contains('processing')) {
                        return;
                    }
                    
                    this.classList.add('processing');
                    this.classList.remove('button-clicked');
                    void this.offsetWidth; // Сбрасываем анимацию
                    this.classList.add('button-clicked');
                    
                    // Отправляем запрос на сервер
                    sendReaction(storyId, isLike, this);
                    
                    // Удаляем блокировку через небольшой таймаут
                    setTimeout(() => {
                        this.classList.remove('processing');
                    }, 800);
                });
                
                // Предотвращаем дрожание при нажатии на кнопки
                button.addEventListener('mousedown', function(e) {
                    e.preventDefault();
                });
            });
            
            // Добавляем обработчик для кнопки комментариев
            document.querySelectorAll('.comment-stats').forEach(function(button) {
                button.addEventListener('click', function() {
                    const storyId = this.closest('.container-story').getAttribute('data-story-id');
                    
                    // Просто сразу перенаправляем на страницу комментариев
                    window.location.href = `/stories/${storyId}/comments/`;
                });
            });
        });
        
        // Функция для переключения активного состояния иконки
        function toggleIconActive(iconElement, active) {
            if (active) {
                iconElement.classList.add('active');
            } else {
                iconElement.classList.remove('active');
            }
        }
        
        // Функция для переключения активного состояния кнопки
        function toggleButtonActive(button, active, type) {
            if (type === 'like') {
                if (active) {
                    button.classList.add('active-like');
                } else {
                    button.classList.remove('active-like');
                }
            } else if (type === 'dislike') {
                if (active) {
                    button.classList.add('active-dislike');
                } else {
                    button.classList.remove('active-dislike');
                }
            }
        }
        
        // Функция для отправки реакции
        function sendReaction(storyId, isLike, clickedButton) {
            // Определяем URL в зависимости от типа реакции
            const url = isLike 
                ? '/stories/ajax/like/' + storyId + '/' 
                : '/stories/ajax/dislike/' + storyId + '/';
            
            // Получаем элементы для обновления
            const container = clickedButton.closest('.story-stats-container');
            const likeButton = container.querySelector('.like-stats');
            const dislikeButton = container.querySelector('.dislike-stats');
            const likeIcon = likeButton.querySelector('.like-icon');
            const dislikeIcon = dislikeButton.querySelector('.dislike-icon');
            const likeCount = likeButton.querySelector('.stats-count');
            const dislikeCount = dislikeButton.querySelector('.stats-count');
            
            // Сохраняем текущие активные состояния
            const wasLikeActive = likeIcon.classList.contains('active');
            const wasDislikeActive = dislikeIcon.classList.contains('active');
            
            fetch(url, {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                    'X-CSRFToken': getCookie('csrftoken')
                },
                credentials: 'same-origin'
            })
            .then(response => response.json())
            .then(data => {
                if (data.status === 'success') {
                    // Если кликнули на лайк
                    if (isLike) {
                        // Если лайк был поставлен
                        toggleIconActive(likeIcon, data.liked);
                        toggleButtonActive(likeButton, data.liked, 'like');
                        
                        // Если был дизлайк и нужно его убрать
                        if (data.was_disliked) {
                            toggleIconActive(dislikeIcon, false);
                            toggleButtonActive(dislikeButton, false, 'dislike');
                            
                            // Анимируем дизлайк только если он был активен и меняем на лайк
                            if (wasDislikeActive) {
                                dislikeButton.classList.remove('button-clicked');
                                void dislikeButton.offsetWidth;
                                dislikeButton.classList.add('button-clicked');
                            }
                        }
                    } 
                    // Если кликнули на дизлайк
                    else {
                        // Если дизлайк был поставлен
                        toggleIconActive(dislikeIcon, data.disliked);
                        toggleButtonActive(dislikeButton, data.disliked, 'dislike');
                        
                        // Если был лайк и нужно его убрать
                        if (data.was_liked) {
                            toggleIconActive(likeIcon, false);
                            toggleButtonActive(likeButton, false, 'like');
                            
                            // Анимируем лайк только если он был активен и меняем на дизлайк
                            if (wasLikeActive) {
                                likeButton.classList.remove('button-clicked');
                                void likeButton.offsetWidth;
                                likeButton.classList.add('button-clicked');
                            }
                        }
                    }
                    
                    // Обновляем счетчики с анимацией
                    updateCountWithAnimation(likeCount, data.likes_count);
                    updateCountWithAnimation(dislikeCount, data.dislikes_count);
                } else {
                    // Обработка ошибки, например, если пользователь не авторизован
                    
                    if (data.message && data.message.includes('авторизуйтесь')) {
                        window.location.href = '/users/login/';
                    }
                }
            })
            .catch(error => {
                console.error('Ошибка:', error);
            });
        }
        
        // Функция для обновления счетчика с анимацией
        function updateCountWithAnimation(countElement, newValue) {
            // Запоминаем оригинальную ширину элемента
            const originalWidth = countElement.offsetWidth;
            
            // Добавляем анимацию
            countElement.classList.add('count-animated');
            
            // Устанавливаем значение
            countElement.textContent = formatLargeNumber(newValue);
            
            // Убираем анимацию через небольшое время
            setTimeout(() => {
                countElement.classList.remove('count-animated');
            }, 400);
        }
        
        // Функция для форматирования больших чисел
        function formatLargeNumber(element) {
            if (typeof element === 'object' && element !== null) {
                const num = parseInt(element.textContent.trim(), 10);
                element.textContent = formatLargeNumber(num);
                return element;
            }
            
            const num = parseInt(element, 10);
            if (num >= 1000000) {
                return (num / 1000000).toFixed(1) + 'М';
            } else if (num >= 1000) {
                return (num / 1000).toFixed(1) + 'К';
            }
            return num.toString().padStart(1, ' ');
        }
        
        // Функция для получения куки
        function getCookie(name) {
            let cookieValue = null;
            if (document.cookie && document.cookie !== '') {
                const cookies = document.cookie.split(';');
                for (let i = 0; i < cookies.length; i++) {
                    const cookie = cookies[i].trim();
                    if (cookie.substring(0, name.length + 1) === (name + '=')) {
                        cookieValue = decodeURIComponent(cookie.substring(name.length + 1));
                        break;
                    }
                }
            }
            return cookieValue;
        }

        // Функция для сохранения текущего состояния
        function saveCurrentState() {
            const currentPage = document.querySelector('.current').textContent;
            const currentSlide = document.getElementById('currentSlide').textContent;
            const urlParams = new URLSearchParams(window.location.search);
            const sort = urlParams.get('sort') || 'new';
            const categories = urlParams.get('categories') || '';

            const state = {
                page: currentPage,
                slide: currentSlide,
                sort: sort,
                categories: categories,
                timestamp: Date.now() // Добавляем временную метку
            };

            localStorage.setItem('storyListState', JSON.stringify(state));
        }

        // Функция для восстановления состояния
        function restoreState() {
            const savedState = localStorage.getItem('storyListState');
            if (!savedState) return null;

            try {
                const state = JSON.parse(savedState);
                
                // Проверяем, не устарело ли состояние (30 секунд)
                if (Date.now() - state.timestamp > 30000) {
                    localStorage.removeItem('storyListState');
                    return null;
                }
                
                return state;
            } catch (e) {
                console.error('Ошибка при восстановлении состояния:', e);
                return null;
            }
        }
    </script>
    
    <!-- JavaScript для работы с выбором нескольких категорий -->
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Добавляем функционал для работы с выбором нескольких категорий
            const multiCategoryToggle = document.getElementById('multiCategoryToggle');
            const categoryArea = document.getElementById('categoryArea');
            const categorySearch = document.getElementById('categorySearch');
            const categoryList = document.getElementById('categoryList');
            const closeCategoryArea = document.getElementById('closeCategoryArea');
            const applyCategoryBtn = document.getElementById('applyCategoryBtn');
            const categorySelectedDisplay = document.getElementById('categorySelectedDisplay');
            const activeFiltersContainer = document.getElementById('activeFiltersContainer');
            const activeFilters = document.getElementById('activeFilters');
            const selectedCategoriesInput = document.getElementById('selectedCategories');
            const selectedSortInput = document.getElementById('selectedSort'); // Получаем ссылку на элемент для сортировки
            
            // Массив для хранения выбранных категорий
            let selectedCategories = [];
            
            // Показать/скрыть выбор категорий
            multiCategoryToggle.addEventListener('click', function() {
                categoryArea.classList.toggle('visible');
                
                // Если открывается окно с выбором категорий, инициализируем его
                if (categoryArea.classList.contains('visible')) {
                    initCategorySelection();
                }
            });
            
            // Закрыть область категорий
            closeCategoryArea.addEventListener('click', function() {
                categoryArea.classList.remove('visible');
            });
            
            // Поиск категорий
            categorySearch.addEventListener('input', function() {
                const query = this.value.toLowerCase();
                
                document.querySelectorAll('.category-item').forEach(item => {
                    const name = item.textContent.toLowerCase();
                    if (name.includes(query)) {
                        item.style.display = '';
                    } else {
                        item.style.display = 'none';
                    }
                });
            });
            
            // Инициализация выбора категорий
            function initCategorySelection() {
                // Если есть ранее выбранные категории, отмечаем их
                document.querySelectorAll('.category-item').forEach(item => {
                    item.classList.remove('selected');
                    
                    // Проверяем, был ли этот элемент выбран ранее
                    const categoryId = parseInt(item.dataset.id);
                    if (selectedCategories.some(cat => cat.id === categoryId)) {
                        item.classList.add('selected');
                    }
                });
                
                // Обновляем отображение выбранных категорий
                updateCategorySelectedDisplay();
            }
            
            // Обработка клика по категории в списке
            document.querySelectorAll('.category-item').forEach(item => {
                item.addEventListener('click', function() {
                    const categoryId = parseInt(this.dataset.id);
                    const categoryName = this.dataset.name;
                    
                    // Если категория уже выбрана, удаляем ее
                    if (this.classList.contains('selected')) {
                        this.classList.remove('selected');
                        selectedCategories = selectedCategories.filter(cat => cat.id !== categoryId);
                        
                        // Удаляем категорию из localStorage
                        localStorage.removeItem('selectedCategories');
                        console.log('Удалена категория из localStorage');
                    } else {
                        // Добавляем категорию в выбранные
                        this.classList.add('selected');
                        selectedCategories.push({ id: categoryId, name: categoryName });
                        
                        // Сохраняем категорию в localStorage
                        localStorage.setItem('selectedCategories', categoryId);
                        console.log('Сохранена категория в localStorage:', categoryId);
                    }
                    
                    // Обновляем отображение выбранных категорий
                    updateCategorySelectedDisplay();
                });
            });
            
            // Обновление отображения выбранных категорий
            function updateCategorySelectedDisplay() {
                if (selectedCategories.length > 0) {
                    const categoryNames = selectedCategories.map(c => c.name).join(', ');
                    categorySelectedDisplay.innerHTML = `<strong>Выбрано ${selectedCategories.length} категорий:</strong> ${categoryNames}`;
                } else {
                    categorySelectedDisplay.innerHTML = '<span class="no-category-selected">Категории не выбраны</span>';
                }
            }
            
            // Применить выбранные категории
            applyCategoryBtn.addEventListener('click', function() {
                // Закрываем окно выбора категорий
                categoryArea.classList.remove('visible');
                
                // Обновляем активные фильтры
                updateActiveFilters();
                
                // Применяем фильтры
                applyMultiCategoryFilters();
            });
            
            // Обновление отображения активных фильтров
            function updateActiveFilters() {
                activeFilters.innerHTML = '';
                
                if (selectedCategories.length > 0) {
                    activeFiltersContainer.style.display = 'flex';
                    
                    selectedCategories.forEach(category => {
                        const filterBtn = document.createElement('span');
                        filterBtn.className = 'category-filter-btn';
                        filterBtn.textContent = category.name;
                        filterBtn.dataset.id = category.id;
                        
                        // Добавляем возможность удаления категории из фильтра
                        filterBtn.addEventListener('click', function() {
                            const categoryId = parseInt(this.dataset.id);
                            selectedCategories = selectedCategories.filter(cat => cat.id !== categoryId);
                            
                            // Удаляем категорию из localStorage
                            localStorage.removeItem('selectedCategories');
                            console.log('Удалена категория из localStorage');
                            
                            // Обновляем активные фильтры
                            updateActiveFilters();
                            
                            // Применяем фильтры
                            applyMultiCategoryFilters();
                        });
                        
                        activeFilters.appendChild(filterBtn);
                    });
                    
                    // Добавляем кнопку "Сбросить все" для очистки всех выбранных категорий
                    if (selectedCategories.length > 1) {
                        const clearAllBtn = document.createElement('span');
                        clearAllBtn.className = 'category-filter-btn clear-all';
                        clearAllBtn.textContent = 'Сбросить все';
                        
                        clearAllBtn.addEventListener('click', function() {
                            selectedCategories = [];
                            
                            // Удаляем категории из localStorage
                            localStorage.removeItem('selectedCategories');
                            console.log('Удалены все категории из localStorage');
                            
                            updateActiveFilters();
                            applyMultiCategoryFilters();
                        });
                        
                        activeFilters.appendChild(clearAllBtn);
                    }
                } else {
                    activeFiltersContainer.style.display = 'none';
                    
                    // Удаляем категории из localStorage, если нет выбранных категорий
                    localStorage.removeItem('selectedCategories');
                    console.log('Удалены все категории из localStorage');
                }
                
                // Сохраняем выбранные категории в скрытое поле
                if (selectedCategoriesInput) {
                    selectedCategoriesInput.value = selectedCategories.map(cat => cat.id).join(',');
                }
            }
            
            // Применение фильтров по нескольким категориям
            function applyMultiCategoryFilters() {
                // Получаем текущие параметры URL
                const urlParams = new URLSearchParams(window.location.search);
                const params = [];
                
                // При применении нового фильтра категорий всегда начинаем с первой страницы,
                // так как набор результатов может измениться
                // НЕ сохраняем параметр страницы при смене категорий
                
                // Добавляем параметр сортировки
                const sort = selectedSortInput.value;
                if (sort && sort !== 'new') {
                    params.push(`sort=${sort}`);
                }
                
                // Добавляем параметры категорий
                if (selectedCategories.length > 0) {
                    const categoryIds = selectedCategories.map(cat => cat.id).join(',');
                    params.push(`categories=${categoryIds}`);
                }
                
                // Формируем URL с параметрами
                let url = window.location.pathname;
                if (params.length > 0) {
                    url += '?' + params.join('&');
                }
                
                // Перенаправляем на новый URL
                window.location.href = url;
            }
            
            // При загрузке страницы проверяем, есть ли выбранные категории в URL
            (function initMultiCategories() {
                const urlParams = new URLSearchParams(window.location.search);
                const categoriesParam = urlParams.get('categories');
                
                if (categoriesParam) {
                    const categoryIds = categoriesParam.split(',').map(id => parseInt(id));
                    
                    // Заполняем массив выбранных категорий
                    document.querySelectorAll('.category-item').forEach(item => {
                        const categoryId = parseInt(item.dataset.id);
                        const categoryName = item.dataset.name;
                        
                        if (categoryIds.includes(categoryId)) {
                            selectedCategories.push({ id: categoryId, name: categoryName });
                        }
                    });
                    
                    // Если есть выбранные категории, обновляем отображение
                    if (selectedCategories.length > 0) {
                        updateActiveFilters();
                    } else {
                        // Если категории указаны в URL, но не найдены в списке категорий
                        // (возможно, некорректный ID), скрываем контейнер
                        activeFiltersContainer.style.display = 'none';
                        
                        // Логируем ошибку для отладки
                        console.warn('Категории указаны в URL, но не найдены в списке категорий:', categoryIds);
                    }
                } else {
                    // Скрываем контейнер, если нет параметров категорий в URL
                    activeFiltersContainer.style.display = 'none';
                }
                
                // Устанавливаем значение скрытого поля для категорий (для совместимости с серверной частью)
                if (selectedCategories.length > 0) {
                    const categoryIds = selectedCategories.map(cat => cat.id).join(',');
                    selectedCategoriesInput.value = categoryIds;
                }
            })();
            
            // Добавляем обработчик для закрытия всплывающего окна по клику вне его области
            document.addEventListener('click', function(e) {
                if (categoryArea.classList.contains('visible') && 
                    !e.target.closest('.category-content') && 
                    !e.target.closest('#multiCategoryToggle')) {
                    categoryArea.classList.remove('visible');
                }
            });
            
            // Добавляем обработчик для клавиши Escape
            document.addEventListener('keydown', function(e) {
                if (e.key === 'Escape' && categoryArea.classList.contains('visible')) {
                    categoryArea.classList.remove('visible');
                }
            });
        });
    </script>
{% endblock %}
