{% extends "base/base.j2" %}


{% block head %}
    <link rel="stylesheet" href="{{ static('css/MainPage.css') }}">
    <link rel="stylesheet" href="{{ static('css/FormContainer.css') }}">
    <link rel="stylesheet" href="{{ static('css/NewStory.css') }}">
    <style>
        .container {
            position: relative;
            padding-bottom: 70px; /* Добавляем отступ снизу для кнопки */
            min-height: 250px;
            max-height: 450px; 
        }
        
        /* Стили для счетчика категорий */
        .category-content {
            position: relative;
            padding-top: 10px;
        }
        
        .category-header {
            display: flex;
            flex-direction: column;
            align-items: center;
            margin-bottom: 15px;
            text-align: center;
        }
        
        .category-title {
            margin-bottom: 8px;
            width: 100%;
            text-align: center;
            font-size: 20px;
        }
        
        .close-category-area {
            position: absolute;
            top: 10px;
            right: 10px;
            font-size: 22px;
            background: none;
            border: none;
            color: #fff;
            cursor: pointer;
            z-index: 5;
            padding: 0;
            line-height: 1;
        }
        
        .category-counter {
            font-size: 14px;
            color: #888;
            padding: 3px 8px;
            border-radius: 12px;
            background: rgba(255, 255, 255, 0.2);
            margin-top: 5px;
        }
        
        .category-counter.limit-close {
            color: #ff9800;
            background: rgba(255, 152, 0, 0.1);
        }
        
        .category-counter.limit-reached {
            color: #ff5252;
            background: rgba(255, 82, 82, 0.1);
        }
        
        /* Адаптивные стили для мобильных устройств */
        @media screen and (max-width: 768px) {
            .main-title {
                font-size: 24px;
                margin-bottom: 10px;
            }
            
            .main-description {
                font-size: 14px;
                line-height: 1.4;
            }
            
            .main-info-text p[style*="color: #ffcc00"] {
                height: auto !important;
            }
            
            .story-title-container, .story-history-container {
                font-size: 16px;
            }
            
            .story-title-display, .story-content-display {
                font-size: 15px;
            }
            
            .story-title-input, .story-content-input {
                font-size: 15px;
            }
            
            .container-submit {
                display: flex;
                justify-content: center;
                align-items: center;
                width: 100%;
                margin-top: 15px;
                position: absolute;
                bottom: 10px;
                left: 0;
                right: 0;
            }
            
            .container-submit button {
                width: 80%;
                font-size: 16px;
                padding: 10px;
                max-width: 300px;
                margin: 0 auto;
            }
            
            .story-media-files, .story-category {
                font-size: 12px;
            }
            
            .file-upload-label {
                font-size: 14px;
                padding: 50px!important;
            }
            
            .category-title {
                font-size: 18px;
            }
            
            .category-item {
                font-size: 14px;
                padding: 8px;
            }
        }
    </style>
{% endblock %}

{% block content %}
    <div class="main-info">
        <div class="main-info-text">
        <h1 class="main-title">НОВАЯ ИСТОРИЯ</h1>
        <p class="main-description">Расскажи, как ты вляпался в очередную нелепую ситуацию,
и получи шанс на вознаграждение за не страховой случай 😍.</p>
        <p class="main-description" style="color: #ffcc00; font-size: 14px; font-style: italic; height: 10px;">Важно: история появится на сайте только после одобрения администратором.</p>
        </div>
        <div class="container">
            <form method="post" id="storyForm" enctype="multipart/form-data">
                {{ csrf_input }}
                <div class="story-title-container">
                    <div class="story-title-display placeholder" id="titleDisplay">Нажмите, чтобы ввести заголовок</div>
                    <input type="text" name="title" id="titleInput" class="story-title-input" maxlength="52" placeholder="Введите заголовок истории">
                    <div class="word-counter" id="charCounter">0/52 символов</div>
                </div>
                
                <div class="story-history-container">
                    <div class="story-content-display placeholder" id="contentDisplay">Нажмите, чтобы ввести содержание истории</div>
                    <div id="contentInput" class="story-content-input" contenteditable="true" placeholder="Расскажите свою историю здесь..."></div>
                    <input type="hidden" name="content" id="hiddenContentInput">
                </div>
                <div class="story-media-info-container">
                <div class="story-media-files" id="mediaToggle">Медиа<span id="mediaText">: файлы подтверждающие фейл</span></div>
                <div class="file-upload-area" id="fileUploadArea">
                    <div class="file-upload-content">
                        <button class="close-upload-area" id="closeUploadArea">&times;</button>
                        <div class="file-counter" id="fileCounter">0/6 файлов</div>
                        <label class="file-upload-label">
                            Перетащите файлы сюда или нажмите для выбора
                            <input type="file" name="media_files" multiple class="file-upload-input" id="fileUploadInput" accept="image/*">
                        </label>
                        <div class="file-preview" id="filePreview"></div>
                    </div>
                </div>
                
                <div class="story-category" id="categoryToggle">Категория<span id="categoryText">: выберите категорию</span></div>
                <div class="category-area" id="categoryArea">
                    <div class="category-content">
                        <button class="close-category-area" id="closeCategoryArea">&times;</button>
                        <div class="category-header">
                            <h3 class="category-title">Выберите категории</h3>
                            <div class="category-counter" id="categoryCounter">0/3 категории</div>
                        </div>
                        <input type="text" class="category-search-box" id="categorySearch" placeholder="Поиск категории...">
                        <div class="category-list-container">
                            <ul class="category-list" id="categoryList">
                                <!-- Список категорий будет загружен динамически -->
                            </ul>
                        </div>
                        <input type="hidden" name="categories" id="selectedCategoriesInput">
                        <div class="category-selected-display" id="categorySelectedDisplay">
                            <span class="no-category-selected">Категории не выбраны</span>
                        </div>
                        <div class="category-actions">
                            <button type="button" class="apply-category" id="applyCategoryBtn">Применить</button>
                        </div>
                    </div>
                </div>
                </div>
                <div class="container-submit">
                    <button type="submit" id="submitButton" disabled>Опубликовать</button>
                </div>
            </form>
        </div>
    </div>
    
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Элементы для заголовка
            const titleDisplay = document.getElementById('titleDisplay');
            const titleInput = document.getElementById('titleInput');
            const charCounter = document.getElementById('charCounter');
            const submitButton = document.getElementById('submitButton');
            const MAX_CHARS = 52;
            
            // Элементы для содержания
            const contentDisplay = document.getElementById('contentDisplay');
            const contentInput = document.getElementById('contentInput');
            const hiddenContentInput = document.getElementById('hiddenContentInput');
            
            // Элементы для категорий
            const categoryToggle = document.getElementById('categoryToggle');
            const categoryArea = document.getElementById('categoryArea');
            const categorySearch = document.getElementById('categorySearch');
            const categoryList = document.getElementById('categoryList');
            const selectedCategoriesInput = document.getElementById('selectedCategoriesInput');
            const categorySelectedDisplay = document.getElementById('categorySelectedDisplay');
            const closeCategoryArea = document.getElementById('closeCategoryArea');
            const applyCategoryBtn = document.getElementById('applyCategoryBtn');
            
            // Массив для хранения выбранных категорий
            let tempSelectedCategories = [];
            
            // Элементы для файлов
            const mediaToggle = document.getElementById('mediaToggle');
            const fileUploadArea = document.getElementById('fileUploadArea');
            const fileUploadInput = document.getElementById('fileUploadInput');
            const filePreview = document.getElementById('filePreview');
            const closeUploadArea = document.getElementById('closeUploadArea');
            const fileCounter = document.getElementById('fileCounter');
            const MAX_FILES = 6;
            
            // Массив для отслеживания текущих файлов
            let currentFiles = [];
            
            // Функция для экранирования HTML-кода
            function encodeHtmlEntities(str) {
                return str.replace(/&/g, '&amp;')
                          .replace(/</g, '&lt;')
                          .replace(/>/g, '&gt;')
                          .replace(/"/g, '&quot;')
                          .replace(/'/g, '&#039;');
            }
            
            // Применяем экранирование к содержимому ввода
            contentInput.addEventListener('input', function() {
                const encodedText = encodeHtmlEntities(this.innerHTML);
                if (this.innerHTML !== encodedText) {
                    this.innerHTML = encodedText;
                }
                checkFormValidity();
            });
            
            // Функция для проверки заполнения всех обязательных полей
            function checkFormValidity() {
                const titleValue = titleInput.value.trim();
                const contentValue = contentInput.textContent.trim();
                const categoriesValue = selectedCategoriesInput.value;
                
                // Проверяем заполнены ли все поля
                const isTitleValid = titleValue !== '' && titleValue.length <= MAX_CHARS;
                const isContentValid = contentValue !== '';
                const isCategoriesValid = categoriesValue !== '' && categoriesValue.length > 0;
                
                // Обновляем значение скрытого поля
                hiddenContentInput.value = contentInput.innerHTML;
                
                // Активируем или деактивируем кнопку отправки формы
                submitButton.disabled = !(isTitleValid && isContentValid && isCategoriesValid);
            }
            
            // Загрузка категорий с сервера
            function loadCategories() {
                // Используем категории, переданные от сервера через контекст шаблона
                const serverCategories = [
                    {% for category in categories %}
                    {
                        id: {{ category.id }},
                        name: "{{ category.name }}"
                    }{% if not loop.last %},{% endif %}
                    {% endfor %}
                ];
                
                renderCategories(serverCategories);
            }
            
            // Отрисовка списка категорий
            function renderCategories(categories) {
                categoryList.innerHTML = '';
                tempSelectedCategories = [];
                
                // Парсим сохраненные категории из скрытого поля
                const savedCategoryIds = selectedCategoriesInput.value ? 
                    selectedCategoriesInput.value.split(',').map(id => parseInt(id)) : [];
                
                categories.forEach(category => {
                    const li = document.createElement('li');
                    li.className = 'category-item';
                    li.textContent = category.name;
                    li.dataset.id = category.id;
                    li.dataset.name = category.name;
                    
                    // Устанавливаем выбранный элемент, если есть сохраненная категория
                    if (savedCategoryIds.includes(category.id)) {
                        li.classList.add('selected');
                        tempSelectedCategories.push({ id: category.id, name: category.name });
                    }
                    
                    li.addEventListener('click', function() {
                        // Если элемент уже выбран, просто удаляем его из выбранных
                        if (li.classList.contains('selected')) {
                            li.classList.remove('selected');
                            tempSelectedCategories = tempSelectedCategories.filter(c => c.id !== category.id);
                            // Обновляем счетчик категорий
                            updateCategoryCounter();
                        } 
                        // Если элемент не выбран и выбрано меньше 3 категорий, добавляем его
                        else if (tempSelectedCategories.length < 3) {
                            li.classList.add('selected');
                            tempSelectedCategories.push({ id: category.id, name: category.name });
                            // Обновляем счетчик категорий
                            updateCategoryCounter();
                        }
                        // Если уже выбрано 3 категории, показываем сообщение или визуальный индикатор
                        else {
                            // Добавляем индикатор ошибки (мигающий красный цвет)
                            const originalColor = li.style.backgroundColor;
                            li.style.backgroundColor = 'rgba(255, 0, 0, 0.3)';
                            setTimeout(() => {
                                li.style.backgroundColor = originalColor;
                            }, 700);
                            
                            // Можно также добавить всплывающее сообщение
                            categorySelectedDisplay.innerHTML = '<span class="no-category-selected" style="color: #ff6b6b;">Можно выбрать максимум 3 категории</span>';
                            setTimeout(() => {
                                updateCategorySelectedDisplay(); // Возвращаем обычный текст через 1.5 секунды
                            }, 1500);
                        }
                        
                        // Обновляем отображение выбранных категорий (если не показываем сообщение об ошибке)
                        if (tempSelectedCategories.length <= 3) {
                            updateCategorySelectedDisplay();
                        }
                    });
                    
                    categoryList.appendChild(li);
                });
                
                // Инициализируем отображение выбранных категорий
                updateCategorySelectedDisplay();
            }
            
            // Поиск категорий
            categorySearch.addEventListener('input', function() {
                const query = this.value.toLowerCase();
                
                // Фильтрация на клиенте (для демо)
                const allCategories = document.querySelectorAll('.category-item');
                allCategories.forEach(item => {
                    const name = item.textContent.toLowerCase();
                    if (name.includes(query)) {
                        item.style.display = '';
                    } else {
                        item.style.display = 'none';
                    }
                });
                
                // В реальном приложении здесь был бы AJAX запрос к серверу
            });
            
            // Показать/скрыть выбор категорий
            categoryToggle.addEventListener('click', function() {
                categoryArea.classList.toggle('visible');
                
                // Если окно открывается, инициализируем временное значение
                if (categoryArea.classList.contains('visible')) {
                    initTempSelectedCategories();
                }
                
                // Если открыт загрузчик файлов, закрываем его
                fileUploadArea.classList.remove('visible');
            });
            
            // Добавляем обработчик и для текстового элемента внутри категории
            const categoryText = document.getElementById('categoryText');
            categoryText.addEventListener('click', function(e) {
                // Предотвращаем всплытие, чтобы не сработал дважды обработчик родителя
                e.stopPropagation();
                // Открываем/закрываем выпадающий список
                categoryArea.classList.toggle('visible');
                
                // Если окно открывается, инициализируем временное значение
                if (categoryArea.classList.contains('visible')) {
                    initTempSelectedCategories();
                }
                
                // Закрываем область медиа если открыта
                fileUploadArea.classList.remove('visible');
            });
            
            // Показать/скрыть загрузку файлов
            mediaToggle.addEventListener('click', function() {
                fileUploadArea.classList.toggle('visible');
                
                // Если открыт выбор категорий, закрываем его
                categoryArea.classList.remove('visible');
            });
            
            // Добавляем обработчик и для текстового элемента внутри медиа
            const mediaText = document.getElementById('mediaText');
            mediaText.addEventListener('click', function(e) {
                // Предотвращаем всплытие, чтобы не сработал дважды обработчик родителя
                e.stopPropagation();
                // Открываем/закрываем область загрузки файлов
                fileUploadArea.classList.toggle('visible');
                // Закрываем выбор категорий если открыт
                categoryArea.classList.remove('visible');
            });
            
            // Закрыть область загрузки файлов по клику на кнопку закрытия
            closeUploadArea.addEventListener('click', function(e) {
                e.preventDefault();
                fileUploadArea.classList.remove('visible');
            });
            
            // Обновление отображения выбранных категорий
            function updateCategorySelectedDisplay() {
                if (tempSelectedCategories.length > 0) {
                    const categoryNames = tempSelectedCategories.map(c => c.name).join(', ');
                    const count = tempSelectedCategories.length;
                    const word = count === 1 ? 'категория' : (count < 5 ? 'категории' : 'категорий');
                    categorySelectedDisplay.innerHTML = `<strong>Выбрано ${count} ${word}:</strong> ${categoryNames}`;
                    
                    // Обновляем счетчик категорий
                    updateCategoryCounter();
                } else {
                    categorySelectedDisplay.innerHTML = '<span class="no-category-selected">Категории не выбраны</span>';
                    
                    // Обновляем счетчик категорий
                    updateCategoryCounter();
                }
            }
            
            // Функция для обновления счетчика категорий
            function updateCategoryCounter() {
                const categoryCounter = document.getElementById('categoryCounter');
                const count = tempSelectedCategories.length;
                categoryCounter.textContent = `${count}/3 ${count === 1 ? 'категория' : (count < 5 ? 'категории' : 'категорий')}`;
                
                // Обновляем стили счетчика в зависимости от заполненности
                if (count >= 3) {
                    categoryCounter.className = 'category-counter limit-reached';
                } else if (count >= 2) {
                    categoryCounter.className = 'category-counter limit-close';
                } else {
                    categoryCounter.className = 'category-counter';
                }
            }
            
            // При выборе категорий обновляем текст
            function updateCategoryText() {
                // Обновляем текст в элементе категории
                if (tempSelectedCategories.length > 0) {
                    const count = tempSelectedCategories.length;
                    categoryText.textContent = `: выбрано ${count} ${count === 1 ? 'категория' : (count < 5 ? 'категории' : 'категорий')}`;
                } else {
                    categoryText.textContent = ': выберите категории';
                }
                
                // Проверяем валидность формы
                checkFormValidity();
            }
            
            // Инициализация временно выбранных категорий
            function initTempSelectedCategories() {
                // Получаем сохраненные ID категорий
                const savedCategoryIds = selectedCategoriesInput.value ? 
                    selectedCategoriesInput.value.split(',').map(id => parseInt(id)) : [];
                
                // Ограничиваем до 3 категорий, если их больше
                const limitedCategoryIds = savedCategoryIds.slice(0, 3);
                
                // Сбрасываем временный массив
                tempSelectedCategories = [];
                
                // Обновляем визуальное отображение выбора
                document.querySelectorAll('.category-item').forEach(item => {
                    const itemId = parseInt(item.dataset.id);
                    if (limitedCategoryIds.includes(itemId)) {
                        item.classList.add('selected');
                        tempSelectedCategories.push({ 
                            id: itemId, 
                            name: item.dataset.name 
                        });
                    } else {
                        item.classList.remove('selected');
                    }
                });
                
                // Обновляем отображение выбранных категорий
                updateCategorySelectedDisplay();
                
                // Обновляем счетчик категорий
                updateCategoryCounter();
            }
            
            // Обработчик для кнопки закрытия категорий
            closeCategoryArea.addEventListener('click', function(e) {
                e.preventDefault();
                
                // Возвращаем предыдущие выбранные категории
                initTempSelectedCategories();
                
                // Закрываем модальное окно
                categoryArea.classList.remove('visible');
            });
            
            // Обработчик для кнопки "Применить"
            applyCategoryBtn.addEventListener('click', function() {
                if (tempSelectedCategories.length > 0) {
                    // Сохраняем выбранные категории
                    selectedCategoriesInput.value = tempSelectedCategories.map(c => c.id).join(',');
                    updateCategoryText();
                } else {
                    // Если категории не выбраны, очищаем значение
                    selectedCategoriesInput.value = '';
                    updateCategoryText();
                }
                
                // Закрываем модальное окно
                categoryArea.classList.remove('visible');
                
                // Проверяем валидность формы
                checkFormValidity();
            });
            
            // Загрузка файлов
            fileUploadInput.addEventListener('change', handleFileUpload);
            
            // Drag & Drop для файлов
            const fileUploadLabel = document.querySelector('.file-upload-label');
            
            ['dragenter', 'dragover', 'dragleave', 'drop'].forEach(eventName => {
                fileUploadLabel.addEventListener(eventName, preventDefaults, false);
            });
            
            function preventDefaults(e) {
                e.preventDefault();
                e.stopPropagation();
            }
            
            ['dragenter', 'dragover'].forEach(eventName => {
                fileUploadLabel.addEventListener('dragenter', highlight, false);
            });
            
            ['dragleave', 'drop'].forEach(eventName => {
                fileUploadLabel.addEventListener(eventName, unhighlight, false);
            });
            
            function highlight() {
                fileUploadLabel.classList.add('highlight');
                fileUploadLabel.style.backgroundColor = 'rgba(255, 255, 255, 0.3)';
                fileUploadLabel.style.borderColor = '#99ff8b';
            }
            
            function unhighlight() {
                fileUploadLabel.classList.remove('highlight');
                fileUploadLabel.style.backgroundColor = 'rgba(255, 255, 255, 0.1)';
                fileUploadLabel.style.borderColor = 'white';
            }
            
            fileUploadLabel.addEventListener('drop', handleDrop, false);
            
            function handleDrop(e) {
                const dt = e.dataTransfer;
                const droppedFiles = dt.files;
                
                // Обновляем список файлов
                if (droppedFiles.length > 0) {
                    // Преобразуем FileList в массив и фильтруем только изображения
                    const newFilesArray = Array.from(droppedFiles).filter(file => file.type.startsWith('image/'));
                    
                    // Проверяем, не превышен ли лимит
                    if (currentFiles.length + newFilesArray.length > MAX_FILES) {
                        
                        
                        // Добавляем только столько файлов, сколько помещается в лимит
                        const availableSlots = MAX_FILES - currentFiles.length;
                        if (availableSlots > 0) {
                            currentFiles = [...currentFiles, ...newFilesArray.slice(0, availableSlots)];
                        }
                    } else {
                        currentFiles = [...currentFiles, ...newFilesArray];
                    }
                    
                    // Обновляем интерфейс
                    updateFilePreview();
                }
            }
            
            // Обновленная функция загрузки файлов
            function handleFileUpload() {
                const files = fileUploadInput.files;
                
                // Обновляем текущий список файлов
                if (files.length > 0) {
                    // Преобразуем FileList в массив для удобства работы
                    const newFilesArray = Array.from(files).filter(file => file.type.startsWith('image/'));
                    
                    // Проверяем лимит файлов
                    if (currentFiles.length + newFilesArray.length > MAX_FILES) {
                       
                        
                        // Берем только столько файлов, сколько помещается в лимит
                        const availableSlots = MAX_FILES - currentFiles.length;
                        if (availableSlots > 0) {
                            currentFiles = [...currentFiles, ...newFilesArray.slice(0, availableSlots)];
                        }
                    } else {
                        currentFiles = [...currentFiles, ...newFilesArray];
                    }
                    
                    // Обновляем интерфейс
                    updateFilePreview();
                }
            }
            
            // Функция для обновления превью файлов
            function updateFilePreview() {
                // Очищаем текущее превью
                filePreview.innerHTML = '';
                
                // Обновляем счетчик файлов
                updateFileCounter();
                
                // Проверяем, достигнут ли лимит
                if (currentFiles.length >= MAX_FILES) {
                    fileUploadInput.disabled = true;
                } else {
                    fileUploadInput.disabled = false;
                }
                
                // Обновляем текст в медиа-переключателе
                if (currentFiles.length > 0) {
                    mediaText.textContent = `: ${currentFiles.length} файл(ов)`;
                } else {
                    mediaText.textContent = ': файлы подтверждающие фейл';
                }
                
                // Создаем превью для каждого файла
                currentFiles.forEach((file, index) => {
                    const reader = new FileReader();
                    
                    reader.onload = function(e) {
                        const previewItem = document.createElement('div');
                        previewItem.className = 'file-preview-item';
                        
                        // Для изображений показываем превью
                        if (file.type.startsWith('image/')) {
                            const img = document.createElement('img');
                            img.src = e.target.result;
                            previewItem.appendChild(img);
                        } else {
                            // Для других файлов показываем название
                            previewItem.textContent = file.name;
                        }
                        
                        // Кнопка удаления
                        const removeBtn = document.createElement('span');
                        removeBtn.className = 'remove-file';
                        removeBtn.innerHTML = '&times;';
                        removeBtn.addEventListener('click', function(e) {
                            e.stopPropagation();
                            
                            // Удаление файла из массива
                            currentFiles.splice(index, 1);
                            updateFilePreview();
                        });
                        
                        previewItem.appendChild(removeBtn);
                        filePreview.appendChild(previewItem);
                    };
                    
                    reader.readAsDataURL(file);
                });
                
                // Создаем и прикрепляем новый DataTransfer объект к input
                updateFileInput();
            }
            
            // Функция для обновления объекта файлового ввода
            function updateFileInput() {
                try {
                    const dataTransfer = new DataTransfer();
                    currentFiles.forEach(file => {
                        dataTransfer.items.add(file);
                    });
                    fileUploadInput.files = dataTransfer.files;
                    console.log("Files updated:", fileUploadInput.files.length, "files ready for upload");
                } catch (e) {
                    console.error("Error updating file input:", e);
                }
            }
            
            // Функция для обновления счетчика файлов
            function updateFileCounter() {
                fileCounter.textContent = `${currentFiles.length}/${MAX_FILES} файлов`;
                
                // Обновляем стили счетчика в зависимости от заполненности
                if (currentFiles.length >= MAX_FILES) {
                    fileCounter.className = 'file-counter limit-reached';
                } else if (currentFiles.length >= MAX_FILES * 0.7) {
                    fileCounter.className = 'file-counter limit-close';
                } else {
                    fileCounter.className = 'file-counter';
                }
            }
            
            // === Логика для содержания истории ===
            // Функция для очистки HTML-тегов
            function stripHtml(html) {
                const tmp = document.createElement('div');
                tmp.innerHTML = html;
                return tmp.textContent || tmp.innerText || '';
            }

            // Показать редактируемый div при клике на текст
            contentDisplay.addEventListener('click', function() {
                contentDisplay.style.display = 'none';
                contentInput.style.display = 'block';
                contentInput.focus();
            });
            
            // Вернуться к отображению текста при потере фокуса
            contentInput.addEventListener('blur', function() {
                // Очищаем HTML-теги перед отображением
                const cleanText = stripHtml(this.innerHTML);
                if (cleanText.trim() !== '') {
                    contentDisplay.textContent = cleanText;
                    contentDisplay.classList.remove('placeholder');
                    contentInput.style.display = 'none';
                    contentDisplay.style.display = 'block';
                } else {
                    contentDisplay.textContent = 'Нажмите, чтобы ввести содержание истории';
                    contentDisplay.classList.add('placeholder');
                    contentInput.style.display = 'none';
                    contentDisplay.style.display = 'block';
                }
                
                checkFormValidity();
            });
            
            // === Логика для заголовка ===
            // Показать поле ввода при клике на текст
            titleDisplay.addEventListener('click', function() {
                titleDisplay.style.display = 'none';
                titleInput.style.display = 'block';
                titleInput.focus();
            });
            
            // Обновить отображаемый текст и счетчик символов при изменении ввода
            titleInput.addEventListener('input', function() {
                const charCount = titleInput.value.length;
                charCounter.textContent = charCount + '/' + MAX_CHARS + ' символов';
                
                // Изменить цвет счетчика символов при приближении к лимиту
                if (charCount >= MAX_CHARS) {
                    charCounter.className = 'word-counter limit-reached';
                } else if (charCount >= MAX_CHARS * 0.8) {
                    charCounter.className = 'word-counter limit-close';
                } else {
                    charCounter.className = 'word-counter';
                }
                
                checkFormValidity();
            });
            
            // Вернуться к отображению текста при потере фокуса
            titleInput.addEventListener('blur', function() {
                titleInput.style.display = 'none';
                titleDisplay.style.display = 'block';
                
                if (titleInput.value.trim() !== '') {
                    titleDisplay.textContent = titleInput.value;
                    titleDisplay.classList.remove('placeholder');
                } else {
                    titleDisplay.textContent = 'Нажмите, чтобы ввести заголовок';
                    titleDisplay.classList.add('placeholder');
                }
                
                checkFormValidity();
            });
            
            // Обработать нажатие Enter
            titleInput.addEventListener('keypress', function(e) {
                if (e.key === 'Enter') {
                    e.preventDefault();
                    titleInput.blur();
                }
            });
            
            // Отправка формы с валидацией
            document.getElementById('storyForm').addEventListener('submit', function(e) {
                e.preventDefault(); // Предотвращаем стандартную отправку формы
                
                const charCount = titleInput.value.length;
                const contentValue = contentInput.textContent.trim();
                const categoriesValue = selectedCategoriesInput.value;
                
                // Убираем класс ошибки с категории
                categoryToggle.classList.remove('error');
                
                // Обновляем скрытое поле перед отправкой
                hiddenContentInput.value = contentInput.innerHTML;
                
                // Проверка обязательных полей
                if (charCount > MAX_CHARS || charCount === 0) {
                    titleDisplay.click();
                    return;
                } else if (contentValue === '') {
                    contentDisplay.click();
                    return;
                } else if (!categoriesValue || categoriesValue === '') {
                    // Добавляем класс ошибки к категории
                    categoryToggle.classList.add('error');
                    // Открываем окно выбора категорий
                    categoryToggle.click();
                    
                    // Убираем класс ошибки через 1.5 секунды
                    setTimeout(() => {
                        categoryToggle.classList.remove('error');
                    }, 1500);
                    return;
                }
                
                // Подготовка файлов для отправки
                const form = document.getElementById('storyForm');
                const formData = new FormData(form);
                
                // Удаляем старые файлы из формы (если есть)
                if (formData.has('media_files')) {
                    formData.delete('media_files');
                }
                
                // Добавляем первые MAX_FILES файлов в форму
                const filesToUpload = currentFiles.slice(0, MAX_FILES);
                filesToUpload.forEach(file => {
                    formData.append('media_files', file);
                });
                
                // Выводим для отладки
                console.log(`Отправляем ${filesToUpload.length} из ${currentFiles.length} файлов`);
                
                // Отправляем форму с помощью fetch API
                fetch(form.action, {
                    method: 'POST',
                    body: formData,
                    credentials: 'same-origin' // для работы с сессиями и CSRF
                })
                .then(response => {
                    if (response.redirected) {
                        // Если сервер перенаправляет (обычное поведение после успешной отправки)
                        window.location.href = response.url;
                    } else {
                        return response.text().then(html => {
                            // Обрабатываем ошибки, если есть
                            if (response.ok) {
                                // Успешно, но без перенаправления
                                window.location.reload();
                            } else {
                                // Показываем сообщение об ошибке
                              
                                console.error('Ошибка отправки:', html);
                            }
                        });
                    }
                })
                .catch(error => {
                    
                    console.error('Ошибка отправки:', error);
                });
            });
            
            // Вызываем функцию загрузки категорий при загрузке страницы
            loadCategories();
            
            // После загрузки категорий обновляем счетчик
            updateCategoryCounter();
            
            // Закрытие выпадающих меню при клике вне их
            document.addEventListener('click', function(e) {
                if (!categoryArea.contains(e.target) && e.target !== categoryToggle) {
                    categoryArea.classList.remove('visible');
                }
                
                // Для закрытия области загрузки файлов при клике на внешнюю область
                if (fileUploadArea.classList.contains('visible') && 
                    e.target === fileUploadArea && 
                    !e.target.closest('.file-upload-content')) {
                    fileUploadArea.classList.remove('visible');
                }
            });
            
            // Предотвращаем всплытие события с области контента загрузки
            document.querySelector('.file-upload-content').addEventListener('click', function(e) {
                e.stopPropagation();
            });
            
            // Инициализация счетчика файлов
            updateFileCounter();
            
            // Обработка нажатия клавиш Escape для закрытия меню
            document.addEventListener('keydown', function(e) {
                if (e.key === 'Escape') {
                    categoryArea.classList.remove('visible');
                    fileUploadArea.classList.remove('visible');
                }
            });
            
            // Инициализируем валидацию формы при загрузке страницы
            checkFormValidity();
        });
    </script>
{% endblock %}
